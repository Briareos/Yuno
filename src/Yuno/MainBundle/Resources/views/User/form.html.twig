{% from 'MainBundle::macros.html.twig' import form_actions %}

<form class="form-horizontal" action="{{ entity.id ? path('user_update', { 'id': entity.id }) : path('user_create') }}" method="post" {{ form_enctype(form) }}>
    <input type="hidden" name="_method" value="{{ entity.id ? 'PUT' : 'POST' }}"/>

    {{ form_widget(form) }}

    {{ form_actions(entity.id, 'user') }}
</form>